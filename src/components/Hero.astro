---
import { Image } from 'astro:assets';
import HeroImage from '../assets/images/hero.png';
---

<section class="relative min-h-screen flex items-center">
  <!-- Background gradient - pointer events none -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary/5 to-accent/5 pointer-events-none"></div>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-20 w-full relative">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Text Content -->
      <div 
        class="text-left relative" 
        data-aos="fade-right" 
        data-aos-duration="1000"
      >
        <h1 class="text-5xl md:text-7xl font-bold text-primary mb-6">
          Tworzymy <span class="text-accent">cyfrowe</span> doświadczenia
        </h1>
        <p class="text-xl text-secondary mb-10">
          Projektujemy i wdrażamy nowoczesne rozwiązania webowe dla firm, które chcą się wyróżnić
        </p>
        <div class="flex flex-wrap gap-4">
          <a
            href="#contact"
            class="group inline-flex items-center px-8 py-3 rounded-full text-lg font-medium bg-accent text-white hover:scale-105 transition-transform"
          >
            <span class="flex items-center">
              Rozpocznij projekt
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </span>
          </a>
          <a
            href="#services"
            class="group inline-flex items-center px-8 py-3 rounded-full text-lg font-medium border-2 border-primary text-primary hover:bg-primary hover:text-white hover:scale-105 transition-all"
          >
            <span class="flex items-center">
              Poznaj nas
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </span>
          </a>
        </div>
      </div>

      <!-- Hero Image Section -->
      <div 
        class="relative"
        data-aos="fade-left" 
        data-aos-duration="1000"
      >
        <!-- Main image container - preserve pointer events -->
        <div class="relative transition-transform hover:-translate-y-2 duration-300">
          <Image
            src={HeroImage}
            alt="Hero Image"
            class="rounded-2xl relative"
            width={800}
            height={600}
            quality={90}
          />
          
          <!-- Shadow overlay - pointer events none -->
          <div 
            class="absolute inset-0 rounded-2xl shadow-2xl pointer-events-none"
            style="background: linear-gradient(to bottom, transparent 50%, rgba(0,0,0,0.1));"
          ></div>
        </div>
        
        <!-- Decorative elements - pointer events none -->
        <div class="absolute -z-1 top-0 right-0 w-96 h-96 bg-accent/5 rounded-full blur-3xl pointer-events-none"></div>
        <div class="absolute -z-1 bottom-0 left-0 w-96 h-96 bg-primary/5 rounded-full blur-3xl pointer-events-none"></div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Ensure section allows pointer events */
  section {
    isolation: isolate;
    pointer-events: auto;
  }

  /* Optimize animations without blocking scroll */
  .blur-3xl {
    animation: float 15s infinite alternate ease-in-out;
    will-change: transform;
    transform: translateZ(0);
    pointer-events: none;
  }

  @keyframes float {
    0%, 100% { transform: translate3d(0, 0, 0); }
    50% { transform: translate3d(-20px, 20px, 0); }
  }
</style>